[tools]
node = "20.19.6"
"aqua:yarnpkg/berry" = "4.4.1"

[tasks.setup]
description = "Install the locked Yarn dependency tree"
run = "yarn install --immutable"

[tasks.lint]
description = "Check TypeScript formatting"
run = "yarn lint"

[tasks.build]
description = "Build the production bundle"
run = "yarn build"
